[project]
name = "vitamin-rule-engine"
version = "0.1.0"
description = "외국인 계절근로자 4종 문서 OCR 구조화 및 R00-R11 교차검증"
requires-python = ">=3.11"

[project.scripts]
vitamin-check = "vitamin.cli:main"
vitamin-server = "vitamin.server:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
